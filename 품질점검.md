# 데이터 품질 점검 리포트

점검일: 2026-07-14 · 대상: `자체감사결과/`, `자체감사파일2/`

공공감사포털(pap.go.kr) API의 현재 상태와 저장소에 저장된 실제 파일을 대조한 무결성·누락 점검 결과입니다.

## 요약

| 항목 | 자체감사결과 | 자체감사파일2 |
|---|---|---|
| 저장 파일 수 | 10,830개 (4.92 GB) | 45,856개 (17.50 GB) |
| 0바이트 파일 | **0** | **0** |
| 손상/HTML 오류페이지 | **0** (전수 검증) | 크기 기준 이상 없음 |
| 보고서 수 (우리) | 2,384건 | 13,476건 |
| 보고서 수 (포털 현재) | 2,406건 | **13,476건 (일치 ✓)** |
| 미수집(HTTP 204) | 0건 | 6건 (기록됨) |

## 1. 무결성 (파일이 온전한가)

- **0바이트 파일: 양쪽 폴더 모두 0개.** GitHub 트리의 blob 크기로 전수 확인.
- **`자체감사결과` 10,830개 전수 매직바이트 검증**(로컬 보관본): PDF `%PDF`, HWP `D0CF11E0`(OLE), HWPX/문서 `PK`(ZIP), PNG/JPG 시그니처를 확인. **손상 파일·HTML 오류페이지 0개.**
- 확장자와 실제 포맷이 다른 파일 23건이 검출됐으나 **손상이 아니라 정상 파일**입니다. 포털 원본 파일명이 `.hwpx`인데 내용은 구형 HWP(OLE)이거나, `.hwp`인데 내용은 ZIP 기반 문서인 경우로, 파일 내용 자체는 유효합니다. (분할 파일의 뒷조각 `_조각(2)....part`는 헤더가 없는 게 정상.)
- 아주 작은 `.txt` 2개(16~19바이트)는 기관이 포털에 올린 실제 짧은 메모입니다: `"임시 업로드용"`, `"보고서 참조"`. 손상이 아닙니다.
- `자체감사파일2`(17.5GB, 원격)는 전량 내려받아 바이트 검증하는 대신 **크기 기준 0바이트 점검만** 수행했습니다(이상 0건). 동일한 수집 스크립트로 저장되었습니다.

## 2. 누락 (빠진 것이 있는가)

- **`자체감사파일2` 기간(2021-01-01 ~ 2025-07-06)**: 포털 현재 보고서 수 **13,476건 = 저장소 13,476건 → 완전 일치.** 보고서 단위 누락 없음.
- **`자체감사결과` 기간(2025-07-07 ~ 2026-07-07)**: 포털 현재 **2,406건** vs 저장소 **2,384건 → 22건 차이.**
  - 날짜 범위는 고정인데 수가 늘어난 것은, 감사보고서가 **수집 시점(2026-07-07경) 이후에 포털에 새로 등록/공개**되면서 이 범위에 편입됐기 때문으로 보입니다.
  - 즉 "빠뜨린 것"이라기보다 **최신화 대상**이며, 증분 수집 스크립트(`_scripts/incremental_scrape.js`)로 채울 수 있습니다.
- **미수집 6건**: 포털이 파일 다운로드 요청에 **HTTP 204(내용 없음)** 로 응답하는 건입니다(메타데이터상 크기는 있으나 실제 바이트를 내주지 않음). 점검일에 재시도해도 동일하게 204였습니다. 저장소 문제나 수집 누락이 아니라 **포털 측에서 파일을 제공하지 않는 것**이며 `_scripts/unavailable2.log`에 전부 기록되어 있습니다.

## 결론

- 저장된 파일은 **온전합니다**(0바이트·손상 없음).
- `자체감사파일2`는 포털과 **건수 완전 일치**, `자체감사결과`는 수집 이후 포털에 추가된 **22건을 증분 수집으로 반영**하면 최신 상태가 됩니다.
- 미수집 6건은 포털이 제공하지 않아 저장 불가능한 항목으로, 원인과 목록이 문서화되어 있습니다.

> 이 리포트의 수치는 점검일(2026-07-14) 기준 포털 API 응답과 저장소 실제 파일을 비교한 값입니다.
