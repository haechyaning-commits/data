# Files exceeding GitHub's 100MB limit — kept locally only, tracked in _scripts/oversized.log

# Transient scratch files (e.g. git push stderr capture)
perr

# Incremental collector's temp staging dir (downloads before integration)
_scripts/.incoming/

# 증분수집으로 새로 받은 파일만 따로 모은 폴더 (자체감사결과에 이미 포함됨)
신규수집_*/
