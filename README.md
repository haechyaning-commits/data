# 공공감사포털 자체감사결과 데이터

[공공감사포털(pap.go.kr)](https://pap.go.kr) 자체감사결과 공개 데이터를 수집한 저장소입니다.

## 이 저장소에서 만든 것

| 파일 | 내용 |
|---|---|
| `자체감사결과/` | 원문 첨부파일 10,830개 (pdf·hwp·hwpx 등) |
| `감사지적_마스터인덱스.csv` | 전 파일을 기관·연도·감사분야·지적제목·처분키워드로 정리한 검색·집계용 인덱스 |
| `감사데이터_대시보드.html` | **통합 대시보드** — 지적사항 검색·집계 + 필터된 원문 파일 열기(한 페이지) |
| `환경부문_감사지적_벤치마크.html` | 환경·에너지 30개 기관 벤치마크 + 한국환경공단 사전점검 체크리스트 |
| `_scripts/` | 위 산출물 생성 스크립트 (`build_index.py`, `build_dashboard.py`, `build_report.py`) |

## 수집 조건

- 시작일: 2025-07-07
- 종료일: 2026-07-07
- 기관분류: 공공기관
- 정렬: 최신순 (API 기본 정렬)
- 총 2,384건의 감사보고서, 첨부파일 10,828개 (10,830개 파일, 그중 1건은 GitHub 100MB 용량 제한으로 2조각 분할)

## 폴더 구조

```
자체감사결과/
  {감사실시기관}_{연도}년 {감사분야}.{확장자}
  {감사실시기관}_{연도}년 {감사분야}(2).{확장자}
  ...
```

- 파일명은 `기관명_연도년 감사분야` 형식입니다 (예: `대한무역투자진흥공사_2026년 종합감사`).
- 감사분야는 API의 `adFldNm` 값(종합감사/특정감사/복무감사/성과감사/재무감사 등)을 사용합니다.
- 같은 기관·연도·감사분야로 여러 건이 등록된 경우, **실제 첨부문서 내용(SHA-256 해시)이 동일하면 번호 없이 1개만 저장**하고, **내용이 다르면 `(1)`, `(2)`... 순서로 번호를 붙여 모두 저장**합니다. 번호는 등록일 기준 최신순으로 매겨집니다.
- 하나의 감사보고서 안에 여러 개의 "처분요구 및 조치사항" 항목이 있는 경우, 각 항목의 첨부파일을 개별적으로 확인하여 실제로 다른 문서인 것만 별도 파일로 저장했습니다 (같은 파일을 공유하는 항목은 중복 저장하지 않음).

## 대용량 파일 안내

`한국동서발전_2026년 특정감사(11).hwpx` 파일은 원본이 GitHub 100MB 업로드 제한을 초과하여 아래 2개로 분할 저장되어 있습니다.

```
한국동서발전_2026년 특정감사(11)_조각(1).hwpx.part
한국동서발전_2026년 특정감사(11)_조각(2).hwpx.part
```

원본으로 복원하려면:

**Windows (명령 프롬프트):**
```
copy /b "한국동서발전_2026년 특정감사(11)_조각(1).hwpx.part"+"한국동서발전_2026년 특정감사(11)_조각(2).hwpx.part" "한국동서발전_2026년 특정감사(11).hwpx"
```

**Mac / Linux:**
```
cat "한국동서발전_2026년 특정감사(11)_조각(1).hwpx.part" "한국동서발전_2026년 특정감사(11)_조각(2).hwpx.part" > "한국동서발전_2026년 특정감사(11).hwpx"
```

## 다운로드 방법

1. 저장소 최상위에서 브랜치를 `claude/audit-portal-data-organization-7v3j4p`로 전환
2. 초록색 **Code** 버튼 → **Download ZIP**
3. 압축 해제 후 `자체감사결과` 폴더 사용

(GitHub 웹 화면은 폴더당 1,000개 파일까지만 미리보기로 표시하지만, ZIP 다운로드나 `git clone`/`git pull`에는 전체 파일이 포함됩니다.)

## 마스터 인덱스 (검색·집계용)

`감사지적_마스터인덱스.csv` — 전체 10,830개 첨부파일을 한 줄씩 정리한 인덱스입니다. 엑셀에서 바로 열어 필터·피벗으로 검색/집계할 수 있습니다.

| 열 | 설명 | 커버리지 |
|---|---|---|
| 기관명 / 연도 / 감사분야 / 순번 | 파일명에서 추출 | 99% |
| 지적제목 | 문서 본문의 "제 목" 항목 (없으면 핵심구 추정) | 96% |
| 처분키워드 | 본문에 등장한 처분유형(징계·주의·통보·시정·개선·회수 등) | 83% |
| 파일형식 / 파일크기(KB) / 분할파일 / 파일명 | 파일 메타데이터 | 100% |

- 감사분야 분포: 종합감사 6,560 · 특정감사 1,827 · 복무감사 1,788 · 성과감사 405 · 재무감사 217
- 재생성: `python3 _scripts/build_index.py` (PyMuPDF·olefile 필요). pdf/hwpx/hwp/txt 본문을 파싱합니다.
- 활용 예: 특정 기관·감사분야 필터링, 처분키워드별 지적 빈도 집계, 환경·에너지 부문 기관만 추려 반복 지적 유형 벤치마킹.

## 통합 대시보드

`감사데이터_대시보드.html` — 검색·집계와 원문 파일 접근을 한 페이지에서 제공하는 자기완결형 도구입니다.

**▶ [브라우저로 바로보기](https://claude.ai/code/artifact/d59a1014-dee7-4381-abe8-b3c2a3a71d6b)**
(어느 컴퓨터에서나 검색·집계가 동작합니다. 원문 '열기'는 온라인에선 GitHub 원문으로 연결되며, 저장소가 비공개면 GitHub 로그인·접근 권한이 필요합니다.)

- **상단**: 기관·연도·감사분야·처분유형·키워드로 지적사항을 필터링하고, 현재 결과의 감사분야·처분 분포가 실시간 집계됩니다.
- **하단**: 필터된 지적 목록과 각 건의 **원문 파일 열기** 링크.
- 파일 열기는 저장소 루트 기준 상대경로(`자체감사결과/…`)라, **저장소를 내려받아 이 HTML을 같은 위치에서 열면** 클릭 시 원문 문서가 바로 열립니다. (GitHub 웹에서는 HTML이 소스로만 보이므로 다운로드 후 사용)
- 재생성: `python3 _scripts/build_dashboard.py`

## 환경·에너지 부문 벤치마크 리포트

`환경부문_감사지적_벤치마크.html` — 마스터 인덱스에서 환경·에너지 관련 30개 기관(수자원공사·발전5사·에너지공단·환경산업기술원·해양환경공단 등)을 추려, 반복 지적 주제·처분유형·기관별 건수를 집계하고 **한국환경공단 사전점검 체크리스트**로 정리한 리포트입니다. 브라우저로 열어 보거나 인쇄/PDF로 저장할 수 있습니다.

**▶ [브라우저로 바로보기](https://claude.ai/code/artifact/0bf89ef3-889f-4b74-a06d-9cc7eea0b421)**
(본인 계정에서 바로 열립니다. 다른 사람과 공유하려면 페이지 우측 상단 공유 메뉴에서 링크 공개로 전환하세요. GitHub 저장소에서는 HTML이 소스로만 보여 렌더링되지 않으므로, 파일을 내려받아 브라우저로 열어도 됩니다.)

- 주요 반복 주제: 시설·공사·안전(1,006) · 정보/개인정보·보안(255) · 계약·용역(254) · 수당·여비·경비(240) 등
- 재생성: `python3 _scripts/build_report.py`

## 수집 방식

공공감사포털의 REST API(`/api/fdadPlanRslt`, `/api/files/filelist`, `/api/files/download`)를 직접 호출하여 실제 존재하는 데이터만 수집했습니다. 임의로 생성하거나 추측한 데이터는 없습니다.
